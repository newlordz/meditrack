{
    "name": "meditrack-railway-deploy",
    "version": "1.0.0",
    "scripts": {
        "install": "npm --prefix client install",
        "build": "npm --prefix client run build",
        "start": "npm --prefix client run preview -- --host 0.0.0.0 --port $PORT"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}